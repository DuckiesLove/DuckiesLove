# Fonts for TalkKink PDF exports

Place `Fredoka-Regular.ttf` (or another preferred typeface) in this folder if you
want the table-based exporter to embed the TalkKink brand font. When the file is
missing, the exporter falls back to the built-in Helvetica font so PDF
generation can still proceed.
